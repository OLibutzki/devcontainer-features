{
    "name": "Claude Code",
    "id": "claude-code",
    "version": "0.0.2",
    "description": "Installs Anthropic's Claude Code CLI using the recommended native installer.",
    "documentationURL": "https://github.com/olibutzki/devcontainer-features/tree/main/src/claude-code",
    "licenseURL": "https://github.com/olibutzki/devcontainer-features/blob/main/LICENSE",
    "keywords": [
        "claude",
        "claude-code",
        "anthropic",
        "ai",
        "cli"
    ],
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "stable"
            ],
            "default": "latest",
            "description": "Release channel ('latest' or 'stable') or an exact version such as '2.1.89'. Must be 2.1.83 or newer: the install fails on an older pin, because CLAUDE_CODE_SUBPROCESS_ENV_SCRUB does not exist before then and the credential would leak into every subprocess Claude Code spawns. The value chosen here also becomes the default auto-update channel."
        },
        "disableAutoUpdater": {
            "type": "boolean",
            "default": false,
            "description": "Set DISABLE_AUTOUPDATER=1 in the user's ~/.claude/settings.json so Claude Code does not update itself in the background. Use this for reproducible images and whenever downloads.claude.ai is not on your egress allowlist."
        },
        "autoOnboarding": {
            "type": "boolean",
            "default": true,
            "description": "Set hasCompletedOnboarding=true in the user's ~/.claude.json so a CLAUDE_CODE_OAUTH_TOKEN or ANTHROPIC_API_KEY login starts a session instead of the interactive theme/login screen (anthropics/claude-code#8938). Set to false to keep the first-run onboarding flow."
        }
    },
    "postStartCommand": "/usr/local/bin/claude-code-onboarding.sh",
    "customizations": {
        "vscode": {
            "extensions": [
                "anthropic.claude-code"
            ]
        },
        "jetbrains": {
            "plugins": [
                "com.anthropic.code.plugin"
            ]
        }
    },
    "containerEnv": {
        "CLAUDE_CODE_SUBPROCESS_ENV_SCRUB": "1"
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
